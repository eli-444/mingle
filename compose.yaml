services:
  app:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      PORT: 3000
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - mingle-data:/app/data
volumes:
  mingle-data:
